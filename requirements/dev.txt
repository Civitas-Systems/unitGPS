# Developer tooling — tests, linting, type checking.
-r core.txt
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10

# Packaging — only needed when running scripts/build_exe.bat to produce
# a standalone Windows .exe of the Streamlit app.
pyinstaller>=6.0
